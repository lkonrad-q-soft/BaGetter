{
  "ApiKey": "",
  "PackageDeletionBehavior": "Unlist",
  "AllowPackageOverwrites": false,
  "MaxPackageSizeGiB": 8,

  "Database": {
    "Type": "Sqlite",
    "ConnectionString": "Data Source=bagetter.db"
  },

  "Storage": {
    "Type": "FileSystem",
    "Path": ""
  },

  "Search": {
    "Type": "Database"
  },

  "Mirror": {
    "Enabled": true,

    // Uncomment this to use the NuGet v2 protocol
    "Legacy": false,
    // Is only used if Legacy=true
    "V2PackageSource": "https://api.nuget.org/v3/index.json",

    // Is only used if Legacy=false
    // At least one mirror feed URL is expected.
    // If more than one is specified, the feeds will be searched in the specified order.
    "V3PackageSources": [
      "https://api.nuget.org/v3/index.json"
    ]
  },

  // Uncomment this to configure BaGetter to listen to port 8080.
  // See: https://docs.microsoft.com/en-us/aspnet/core/fundamentals/servers/kestrel?view=aspnetcore-3.1#listenoptionsusehttps
  //"Kestrel": {
  //  "Endpoints": {
  //    "Http": {
  //      "Url": "http://localhost:8080"
  //    }
  //  }
  //},

  "Logging": {
    "IncludeScopes": false,
    "Debug": {
      "LogLevel": {
        "Default": "Warning"
      }
    },
    "Console": {
      "LogLevel": {
        "Microsoft.Hosting.Lifetime": "Information",
        "Default": "Warning"
      }
    }
  },

  "HealthCheck": {
    "Path": "/health"
  },

  "Statistics": {
    "EnableStatisticsPage": true,
    "ListConfiguredServices": true
  }
}
